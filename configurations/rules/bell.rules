import org.openhab.core.library.types.*
import org.openhab.core.persistence.*
import org.openhab.model.script.actions.*
 
var Timer notificationTimer = null

rule "Bell State" 
when
	Item EG_Vorzimmer_Bell changed
then

	if( EG_Vorzimmer_Bell.state == OPEN ){

		EG_Vorzimmer_Bell_Last_Change.postUpdate( now.toString() )
	        logInfo("Bell rule", "Es hat geklingelt.")

		if( Presence_Garden.state == ON ){
			if(notificationTimer==null) {
				notificationTimer = createTimer(now.plusSeconds(30)) [|
					notificationTimer=null
				]
				pushover("Klingel: Es klingelt")
//				sendMail("wir@cyberkov.at","Es klingelt","Es klingelt jemand an der Tür","https://haus.cyberkov.at/")
			}
		}
	}
end 


