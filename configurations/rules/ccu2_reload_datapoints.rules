//----------------------------------------------------------
// Datapoints shall be reloaded once a day
// according to documentation
//
rule "Reload datapoints"
when
    Time cron "0 0 0/6 * * ?"   // every 6 hours
then
    sendCommand(Reload_Datapoints, ON)
    sendCommand(Reload_Variables, ON)
end
