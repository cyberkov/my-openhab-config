//----------------------------------------------------------
// Some items are not persisted. They should be kept in a
// certain state on startup.
//
rule "Initialize items"
when
        System started
then
  pushover("OpenHAB wurde gestartet.")

  logInfo("Initializer", ""OpenHAB initilized.")
end
