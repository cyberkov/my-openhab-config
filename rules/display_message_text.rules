//----------------------------------------------------------
// Pushover notifications are sent through the script
// to the people currently in the house.
// If no one is here the message is simply passed to
// both.
//
rule "When text changes, show on display"
when
        Item display_message_text received update
then
        logInfo("Displaystatus1", display_message_text.toString)
        logInfo("Displaystatus2", display_message.toString)
        postUpdate(display_message,"{img:sender2_128.png}{backgroundColor:black}{text:<b>Meldungen</b><br>" + display_message_text.state + "}")
        logInfo("Displaystatus3", display_message.toString)
        sendCommand(Reload_Datapoints, ON)
end

